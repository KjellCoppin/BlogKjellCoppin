on:
  push:
    branches:
      - AddRunner
jobs:
  build_and_run:
    name: build and run
    runs-on: ubuntu-latest
    steps:
       - uses: actions/checkout@v4
       
       - name: move into project
         run: cd BlogKjellCoppin

       - name: ls
         run: ls
